import { GroupButton } from "../../ui-kit/GroupButton";
import { Wrapper } from "./styled";

export const Groups = () => {
  return (
    <Wrapper>
      <GroupButton>
        Категории
      </GroupButton>
      <GroupButton>
        ТУ
      </GroupButton>
    </Wrapper>

  );
};